// Don't use this

/* 
function checkDarkTheme() {
    let darkThemeCookie = getCookie("darkTheme");
    if (darkThemeCookie != "") {
      if (darkThemeCookie == "True") {
        body.className = "bodyDark";
        footer.className = "footerDark";
        footerContainer.className = "footerDark footerTextLight";
        themeButton.className = "darkButton";
        themeButton.innerText = "Toggle light!";
        dropShadow.className = "dropShadowDark"
      } else {
        body.className = "bodyLight";
        footerContainer.className = "footerLight footerTextDark";
        footer.className = "footerLight";
        themeButton.className = "lightButton";
        themeButton.innerText = "Toggle dark!";
        dropShadow.className = "dropShadowLight";
      }
    } else {
      setCookie("darkTheme", "True", 365);
    }
}



function changeChars() {
    iter += 1

    for (var i = 0; i < shuffleValue; i++) {
        var randomCharacter = random(obfuscatorChars);
        errorCode = setCharAt(errorCode, i, randomCharacter);
    }
    document.getElementById("errorCode").innerText = errorCode;
    if (iter >= 160) {
        document.getElementById("errorCode").innerText = "404";
        clearInterval(intervalId);
    }
}



async function textPopup() {
    var errorCode = document.getElementById("errorCode");
    errorCode.innerText = "404|";
    await sleep(160);
    errorCode.innerText = "4|";
    await sleep(160);
    errorCode.innerText = "";
    await sleep(160);
    errorCode.innerText = "40";
    await sleep(160);
    errorCode.innerText = "?|";
    await sleep(160);
    errorCode.innerText = "??";
    await sleep(160);
    errorCode.innerText = "???|";
    await sleep(160);
    errorCode.innerText = "??";
    await sleep(160);
    errorCode.innerText = "?|";
    await sleep(160);
    errorCode.innerText = "";
    await sleep(160);
    errorCode.innerText = "4|";
    await sleep(160);
    errorCode.innerText = "40";
    await sleep(160);
    errorCode.innerText = "404|";
    await sleep(160);
    errorCode.innerText = "404";
}


*/